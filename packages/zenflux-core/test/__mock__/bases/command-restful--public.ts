import * as ZenCore from "@zenflux/core/src/exports";

export class __CommandRestful__Public__ extends ZenCore.commandBases.CommandRestful {
    public apply( args: any = {}, options: any = {} ) {
        return super.apply( args, options );
    }
}
