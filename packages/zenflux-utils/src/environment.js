/**
 * @author Leonid Vinikov <leonidvinikov@gmail.com>
 */

/**
 * Checks if debug is enabled for the given type and name.
 *
 * @param {string} type
 * @param {string} name
 * @return {boolean}
 */
export function isDebugEnabled( type, name ) {
    return false;
}

