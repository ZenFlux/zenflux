import { clientRenderBoundary } from "@zenflux/react-dom-bindings/src/server/fizz-instruction-set/ReactDOMFizzInstructionSetInlineSource";
// This is a string so Closure's advanced compilation mode doesn't mangle it.

window[ "$RX" ] = clientRenderBoundary;
